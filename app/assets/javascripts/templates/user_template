<script id="user-template" type="text/x-handlebars-template">
	<div class="well">
		<h4>{{matchArray[ii].name}}</h4>
		<a href='/user/'+{{id}}>See Profile</a>
		<p>{{matchArray[ii].points}}</p>
		<p>{{matchArray.teamNames}}</p>
	</div>
</script>
