<%= form_for @team do |f| %>
  <div class="control-group">
    <%= f.label :name, "Team Name" %>
    <%= f.text_field :name %>
  </div>
  <br>
  <div class="form-actions">
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
